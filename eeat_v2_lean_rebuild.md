# EEAT Pro Studio v2 — แผนสร้างระบบใหม่ (Lean Rebuild)

> เอกสารออกแบบระบบเวอร์ชันใหม่ที่เล็กลง เน้น "เขียนได้งานจริง"
> ตัดความซับซ้อนที่ทำให้ระบบเดิมพัง · เวอร์ชัน 1.0

---

## สารบัญ

1. [หลักการออกแบบใหม่](#1-หลักการออกแบบใหม่)
2. [ขอบเขต — เก็บอะไร ตัดอะไร](#2-ขอบเขต)
3. [7 โมดูลของระบบใหม่](#3-7-โมดูลของระบบใหม่)
4. [หัวใจ: Flow เขียนบทความใหม่](#4-หัวใจ-flow-เขียนบทความใหม่)
5. [แผน migration (เก็บ/สร้างใหม่)](#5-แผน-migration)
6. [Roadmap การสร้าง](#6-roadmap-การสร้าง)
7. [แผนขยายในอนาคต](#7-แผนขยายในอนาคต)

---

## 1. หลักการออกแบบใหม่

### บทเรียนจากระบบเดิม

ระบบเดิมพังเพราะ **"ทำเยอะเกินไปพร้อมกัน"** — 25 เมนู, 10 step, ระบบวัดผล 4 ชั้น, batch, AEO — ทุกอย่างพันกันจนหาจุดพังไม่เจอ และ "ท่อ" ระหว่างส่วนต่างๆ ไม่เชื่อม

### หลักการใหม่: 3 ข้อ

1. **เขียนได้จริงก่อน อย่างอื่นทีหลัง** — เป้าหมายเดียวของ v1: กด → ได้บทความสมบูรณ์ที่คนแค่ตรวจ/แก้เล็กน้อย
2. **เรียบง่ายกว่าครบถ้วน** — ตัด feature ที่ไม่จำเป็นออกให้หมด เหลือ 7 โมดูลแก่น
3. **ทำทีละส่วนให้เสร็จจริง** — สร้างโมดูลเดียวให้ทำงาน 100% ก่อนทำตัวถัดไป (ไม่สร้างครึ่งๆ หลายตัว)

### สิ่งที่เปลี่ยนจากเดิม

| เดิม | ใหม่ |
|---|---|
| 25 เมนู | 7 โมดูล |
| flow เขียน 10 step | pipeline เรียบง่าย (เขียนจบในครั้งเดียว) |
| ระบบวัดผล 4 ชั้น (EEAT/Gate/XACT/AEO) | ตัดออก (เพิ่มทีหลังถ้าต้องการ) |
| batch 1,724 ค้าง | ตัดออก (เพิ่มทีหลัง) |
| research layer + citation | ตัดออก v1 (เพิ่ม v2) |
| คลังผู้เชี่ยวชาญปลอม | ตัดออก (ใช้ผู้เขียนจริง) |

---

## 2. ขอบเขต

### ✅ เก็บ (7 โมดูลที่ต้องการ)

1. **เขียนบทความ** — AI เขียนจบ คนตรวจ/แก้เล็กน้อย
2. **จัดการบทความ** — list, ค้นหา, แก้ไข, ลบ, สถานะ
3. **Keyword Cluster Planner** — Pillar/Cluster/Supporting + Tree view
4. **โปรเจกต์บทความ** — 1 ลูกค้า = 1 โปรเจกต์
5. **จัดการสมาชิก** — RBAC (Admin/Writer)
6. **จัดการทีม** — แชร์งานในทีม
7. **ตั้งค่าระบบ** — API keys (LLM provider)

### ❌ ตัดออก (จากระบบเดิม)

- Batch สร้างหลายชิ้น (ค้าง 1,724 — เพิ่มทีหลังเมื่อ pipeline นิ่ง)
- AEO Optimizer / AEO Trend / ประวัติ AEO (4 หน้ากระจาย)
- Topical Map (ซ้ำกับ Keyword Cluster)
- Trending Keywords (พัง — Google Trends ไม่มี API)
- วิเคราะห์คู่แข่ง / วิเคราะห์ Keyword (research — เพิ่ม v2)
- Internal Link Suggester / Map (เพิ่มทีหลัง)
- คลังผู้เชี่ยวชาญ (E-E-A-T ปลอม — เสี่ยง)
- คลังแหล่งอ้างอิง (research — เพิ่ม v2)
- คลัง Keyword (รวมเข้า Keyword Cluster)
- ระบบวัดผล 4 ชั้น (Quality Gate/XACT/EEAT Score)
- รายงาน/สถิติ/Cache (เพิ่มทีหลัง)

> **หมายเหตุ:** "ตัดออก" = ไม่อยู่ใน v1 ไม่ได้แปลว่าทิ้งถาวร — หลาย feature ดี แค่ยังไม่ใช่ตอนนี้ เพิ่มกลับได้เมื่อแก่นทำงานแล้ว (ดูหัวข้อ 7)

---

## 3. 7 โมดูลของระบบใหม่

### โครงสร้างเมนู (เรียบง่าย)

```
สร้างงาน:
  ├─ เขียนบทความ
  └─ จัดการบทความ

วางแผน:
  ├─ โปรเจกต์บทความ
  └─ Keyword Cluster Planner

ระบบ:
  ├─ จัดการสมาชิก
  ├─ จัดการทีม
  └─ ตั้งค่าระบบ
```

### โมดูล 1 · เขียนบทความ ⭐ (หัวใจ — ดูรายละเอียดหัวข้อ 4)

**เป้าหมาย:** กด → ได้บทความสมบูรณ์ (AI เขียนจบ คนแค่ตรวจ)

**Input:** keyword + หัวข้อ + หมวด + ผู้เขียน (จากทีมจริง)
**Output:** บทความ markdown + meta (title/description) สมบูรณ์

### โมดูล 2 · จัดการบทความ

- List บทความทั้งหมด (มี pagination — แก้ปัญหาเดิม)
- ค้นหา + filter (สถานะ/หมวด/โปรเจกต์)
- แก้ไข / ลบ / เปลี่ยนสถานะ (ร่าง/เผยแพร่)
- ผูกกับโปรเจกต์ + ผู้เขียน

### โมดูล 3 · Keyword Cluster Planner

**เก็บของเดิมที่ดี:**
- โครงสร้าง Pillar / Cluster / Supporting
- Tree view (ความสัมพันธ์)
- intent tag รายคำ
- สถานะ "เขียนแล้ว/ยังไม่เขียน"
- ปุ่ม "เขียน" → **ส่ง keyword + intent เข้าโมดูล 1** (แก้ท่อขาดเดิม)

### โมดูล 4 · โปรเจกต์บทความ

- 1 โปรเจกต์ = 1 ลูกค้า/เว็บ
- ผูก: keyword หลัก + หมวด + บทความ + Keyword Cluster
- แก้ปัญหาเดิม: แสดงวันที่/batch แยกได้ (กันชื่อซ้ำสับสน)

### โมดูล 5 · จัดการสมาชิก

**เก็บของเดิม (ทำงานดี):**
- RBAC: Admin / Writer
- Google OAuth login
- เพิ่ม/ลบ/เปลี่ยน role
- แก้: ลบ DebugTestUser + unique email

### โมดูล 6 · จัดการทีม

**เก็บของเดิม:**
- แต่ละคนสร้างทีมได้ (multi-tenant)
- แชร์บทความ + Keyword Cluster ในทีม
- เพิ่ม: permission scope (read/edit) ให้ชัด

### โมดูล 7 · ตั้งค่าระบบ

**เก็บของเดิม (architecture ดี):**
- API key หลาย provider + encrypted (DB > .env)
- LLM: OpenRouter (หลัก) + backup
- **v1 ต้องการแค่ LLM key** (ยังไม่ต้อง SERP — ตัด research ออก)

---

## 4. หัวใจ: Flow เขียนบทความใหม่

> นี่คือส่วนที่ต้องรื้อสร้างใหม่ทั้งหมด เพราะ 10 step เดิมพัง

### หลักการ: เรียบง่าย + เขียนจบในครั้งเดียว + ไม่พังกลางทาง

### Pipeline ใหม่ (5 ขั้น — จาก 10 step)

```
1. Input      → keyword + หัวข้อ + หมวด + ผู้เขียน
2. Outline    → AI สร้างโครงเรื่อง (H2/H3) จาก intent
3. Write      → AI เขียนเนื้อหาทีละ section (กันพังกลางทาง)
4. Assemble   → รวม section + gen meta (title/description)
5. Review     → แสดงให้คนตรวจ/แก้ → บันทึก
```

### จุดสำคัญที่แก้จากเดิม

**1. เขียนทีละ section (แก้ปัญหา "พังกลางทาง")**
- เดิม: เขียนรวดเดียว → ถ้า LLM ตอบไม่ครบ/timeout = พังทั้งบทความ
- ใหม่: เขียนทีละ H2 → ถ้า section ไหนพัง retry เฉพาะอันนั้น → บทความไม่พังทั้งหมด

**2. มี retry limit + error handling ชัด (แก้ปัญหา 402/เผาเครดิต)**
- retry สูงสุด 2 ครั้งต่อ section
- เจอ 402/เครดิตหมด → หยุดทันที + แจ้งผู้ใช้ (ไม่ retry รัว)
- แสดง progress ทีละ section (ผู้ใช้เห็นว่าถึงไหน)

**3. ตัด keyword density แบบตัวเลขตายตัว**
- เดิม: บังคับ "ใส่ N ครั้ง" → บทความแข็ง
- ใหม่: prompt สั่ง "ใช้ keyword ธรรมชาติ" + คุมแค่ให้อยู่ใน H1/intro/H2 (ตำแหน่งสำคัญ)

**4. คุมความยาวประโยค (แก้ปัญหา readability)**
- prompt สั่ง: ประโยค < 25 คำ, ย่อหน้าสั้น อ่านง่าย

**5. ไม่มี placeholder หลุด (แก้ปัญหา `**` เดิม)**
- validate output ก่อนแสดง: ไม่มีตัวแปรที่ไม่ถูกแทนค่า

### Prompt เขียน (โครงสร้างแนะนำ)

```
System: คุณเป็นนักเขียนบทความภาษาไทยมืออาชีพ หมวด {category}
        เขียนให้อ่านง่าย ประโยคสั้น (<25 คำ) เป็นธรรมชาติ

User:   หัวข้อ: {title}
        Keyword หลัก: {keyword} (ใช้ธรรมชาติ ห้ามยัด)
        เขียน section: {h2_heading}
        ความยาว: {word_count} คำ
        โทน: ให้ข้อมูล/ความรู้
```

> **หมายเหตุ v1:** ยังไม่ใส่ citation/research — AI เขียนจากความรู้ทั่วไป + คนตรวจ (ตามที่เลือก "เพิ่ม research ทีหลัง")

### ⚠️ ข้อจำกัดที่ต้องยอมรับใน v1

เพราะตัด research ออก → บทความ v1 จะ:
- ✅ อ่านลื่น เขียนจบ มีโครงดี
- ⚠️ **ไม่มี citation/สถิติจริง** (AI เขียนจากความรู้ทั่วไป)
- → **คนต้องตรวจ + เติมข้อมูลจริงเอง** (ตรงกับ workflow "คนตรวจ/แก้")

**นี่คือ trade-off ที่ยอมรับได้** เพื่อให้ระบบทำงานจริงก่อน แล้วค่อยเพิ่ม research ใน v2

---

## 5. แผน migration

### เก็บโค้ดเดิม (ประหยัดเวลา)

| ส่วน | สถานะเดิม | ทำอะไร |
|---|---|---|
| Auth (Google login) | ✅ ดี | เก็บ ใช้ต่อ |
| RBAC + สมาชิก | ✅ ดี | เก็บ + ลบ test data |
| จัดการทีม | ✅ ดี | เก็บ + เพิ่ม permission scope |
| ตั้งค่าระบบ (API) | ✅ ดี | เก็บ (เอาแค่ LLM key ส่วน) |
| Keyword Cluster | ✅ ดี | เก็บ + แก้ปุ่มเขียนให้ส่ง intent |
| โปรเจกต์ | ✅ ใช้ได้ | เก็บ + แก้เรื่องแยก batch |

### สร้างใหม่

| ส่วน | ทำไม |
|---|---|
| **Flow เขียนบทความ** | เดิมพัง — รื้อเป็น pipeline 5 ขั้น (หัวข้อ 4) |
| จัดการบทความ | เดิมไม่มี pagination — ทำใหม่ให้ดี |

### แก้บั๊กข้ามระบบ (ทำครั้งเดียว)

- วันที่ 1/1/2513 → migrate + fix ที่ model layer
- record ซ้ำ → unique constraint
- category hardcode → ย้ายเป็น database table (ดูหมายเหตุด้านล่าง)

> **แนะนำเพิ่ม:** ทำ **table categories** (จากเอกสาร refactor ก่อนหน้า) ตั้งแต่ v1 เลย — เพราะทั้ง 7 โมดูลใช้หมวด ถ้าทำ database-driven ตั้งแต่แรกจะเพิ่มหมวดเองได้ ไม่ต้อง hardcode

---

## 6. Roadmap การสร้าง

### หลักการ: ทำทีละโมดูลให้เสร็จจริง

**Phase 1 — ฐาน (เก็บของเดิม + เตรียม)**
1. ยืนยันส่วนที่เก็บทำงานได้ (auth/สมาชิก/ทีม/ตั้งค่า)
2. ทำ table categories (แก้ hardcode)
3. แก้บั๊ก database (วันที่/record ซ้ำ)

**Phase 2 — หัวใจ (เขียนบทความ)**
4. สร้าง pipeline เขียนใหม่ (5 ขั้น) — **ทำให้เขียนจบจริง 1 บทความก่อน**
5. เขียนทีละ section + retry + error handling
6. gen meta (title/description)
7. **ทดสอบ: เขียน 10 บทความจริง ดูว่าจบครบ ไม่พัง**

**Phase 3 — รอบระบบ**
8. จัดการบทความ (list/edit/pagination)
9. เชื่อม Keyword Cluster → ปุ่มเขียน → pipeline
10. เชื่อมโปรเจกต์

**Phase 4 — ขัดเงา**
11. permission scope ทีม
12. เก็บกวาด UX

> **กฎเหล็ก:** อย่าไป Phase ถัดไปจนกว่า Phase ปัจจุบันทำงานจริง 100% — บทเรียนจากระบบเดิมที่สร้างทุกอย่างครึ่งๆ

---

## 7. แผนขยายในอนาคต (v2, v3)

เมื่อแก่น (v1) ทำงานจริงแล้ว ค่อยเพิ่มทีละอย่าง:

### v2 — เพิ่ม Research (คุณภาพบทความ)
- ต่อ Serper (มี key แล้ว) → ดึง SERP
- เติมคลังแหล่งอ้างอิงจาก organic URLs
- ให้ pipeline เขียนใส่ citation จริง
- → บทความมี E-E-A-T จริง (ไม่ต้องคนเติมเอง)

### v3 — เพิ่ม Scale + วัดผล
- Batch (เมื่อ pipeline นิ่งแล้ว ไม่ค้าง)
- ระบบวัดผล (เอาแค่ที่จำเป็น ไม่ 4 ชั้น)
- Internal linking อัตโนมัติ

### สิ่งที่ "ไม่ควรเพิ่มกลับ"
- คลังผู้เชี่ยวชาญปลอม (E-E-A-T ปลอม — เสี่ยง)
- ระบบวัดผลซ้อน 4 ชั้น (ซับซ้อนเกิน)
- Trending Keywords (Google Trends ไม่เสถียร)

---

## สรุป

**v1 = ระบบเล็กที่ทำงานได้จริง** — 7 โมดูล, pipeline เขียนเรียบง่ายที่เขียนจบไม่พัง, เก็บของเดิมที่ดี, สร้างใหม่เฉพาะส่วนที่พัง

**หัวใจคือ:** ทำให้ "เขียนบทความได้งานจริง" สำเร็จก่อน (Phase 2) — ทุกอย่างอื่นรอได้

**trade-off ที่ยอมรับ:** v1 ไม่มี citation (คนเติมเอง) แลกกับระบบที่ทำงานจริง แล้วเพิ่ม research ใน v2

> เอกสารนี้ทำงานคู่กับรายงานตรวจสอบเดิม (audit report) — ใช้อ้างอิงบั๊ก/ปัญหาที่เจอตอนวิเคราะห์ระบบเก่า
